# PhoSnap 전체 플로우 검증 리포트

작성일: 2026-09-10 (1차) · **2026-09-11 갱신 (2차 — 브라우저 실검증)**
범위: 작가 · 헤메 · 벤더 · 고객 4개 역할의 실제 서비스 시나리오 검증
방법: 실제 계정으로 가입 → 등록 → 예약 → 승인 → 채팅 → 알림까지 브라우저에서 직접 수행

> **2차 검증 요약 (2026-09-11)**
> 예약 구조를 `booking_items` 로 개편한 뒤 실제 브라우저로 전 구간을 다시 돌렸다.
> **10건을 추가로 찾았고, 그중 8건은 실제 데이터를 넣고 실제 버튼을 눌러야만
> 드러나는 것들이었다.** 가장 큰 것은 `sendNotificationTo()` 가 한 번도
> 작동한 적이 없었다는 사실이다. 상세는 문서 하단 "2차 검증" 절 참조.

---

## 요약

DB를 비우고 mock 데이터를 걷어낸 상태에서 실제 플로우를 돌려본 결과,
**어느 역할도 자기 데이터를 고객에게 전달할 수 없는 상태**였다.

- 작가는 가입해도 검색 결과에 나타나지 않음
- 벤더는 의상을 등록할 방법 자체가 없음
- 헤메는 대시보드가 영어로 뜨고 시술 메뉴 저장이 실패
- 고객은 자기 예약을 마이페이지에서 볼 수 없음
- 예약·채팅·알림이 전부 동작하지 않음

**61개 이슈를 찾아 수정했고, 지금은 4개 역할 전 구간이 실제로 연결된다.**

---

## 왜 지금까지 안 보였나

버그 자체보다 **버그를 가리는 구조**가 문제였다. 네 가지 패턴이 반복됐다.

| 패턴 | 설명 | 사례 |
|---|---|---|
| mock 폴백 | DB가 비면 하드코딩 데이터로 대체 | 작가 목록, 헤메 선택, 벤더 아이템, 고객 예약 내역 |
| 조용한 실패 | `catch {}`, `// silently handled` | 의상 등록·삭제, 이미지 업로드, 알림 발송 |
| 없는 컬럼에 쓰기 | Supabase가 0건 갱신으로 넘어가고 에러 없음 | `is_read`, `vendor_type`, `size_stock`, `display_name` |
| 무조건 성공 표시 | 저장 안 됐는데 성공 팝업 | 벤더 프로필 "Mock mode" |

`localStorage.clear()` + DB 비우기를 하지 않았다면 대부분 계속 숨어 있었을 것이다.

---

## 재검토에서 추가 발견 (5건)

첫 검증을 마친 뒤 다시 훑으면서 5개를 더 찾았다.
**"데이터가 없어서 통과한 검증은 검증이 아니었다"**는 것이 핵심 교훈이다.

| # | 내용 | 왜 처음에 안 보였나 |
|---|---|---|
| 57 | 국가 필터 카운트가 mock 기준 ("한국 4", 실제 1명) | 숫자만 보고 지나침 |
| 58 | **리뷰 저장 실패 3종을 전부 `.catch(() => {})` 로 삼킴** | 무조건 "저장되었습니다 ✓" 표시 |
| 59 | **실제 헤메 데이터가 들어오자 예약 페이지 크래시** | 검증 시점엔 헤메가 0명이라 카드가 렌더될 일이 없었음 |
| 60 | 헤메 카드 가격이 ₩0, 지역이 raw id | 같은 이유 |
| 61 | DressCard `sizes.join()` 방어 없음 | 같은 이유 |

59번이 특히 중요하다. 예약 플로우를 "검증 완료"로 표시했지만, 그때는
헤메·의상이 하나도 등록되지 않아 3~5단계가 전부 "없음"으로 넘어갔다.
실제 데이터를 넣자마자 `s.languages.slice()` 에서 페이지 전체가 죽었다.
`languages` / `tags` 는 mock 스타일리스트에만 있던 필드였다.

---

## 이슈 분류 (61건)

### A. DB 설정 누락 — SQL로 해결 (14건)

| # | 내용 |
|---|---|
| 1 | `profiles` RLS 무한 재귀(42P17) — 모든 조회 실패의 근원 |
| 2 | `stylists`/`dress_vendors`/`venue_vendors` 권한 누락 |
| 3 | `profiles` 컬럼 대량 누락 → 실명·전화·주소·포트폴리오 유실 |
| 4 | `photographers` INSERT 정책 부재 |
| 5 | SELECT 정책이 `is_active=true`뿐 → 본인도 조회 불가 |
| 6 | 누락 테이블 3개 (`user_roles`, `notifications`, `vendor_reviews`) |
| 7 | **Storage 버킷 0개** — 모든 사진 업로드 불가 |
| 8 | `notifications`에 `link`/`metadata` 컬럼 없음 |
| 9 | `chat_rooms` INSERT 정책 부재 |
| 10 | 채팅 RLS가 `auth.uid() = photographer_id` 비교 (타입 불일치) |
| 11 | `notifications`/`messages` DELETE 정책 부재 |
| 12 | `dress_vendors` 가입 payload 컬럼 전부 누락 |
| 13 | `dress_items.size_stock` 컬럼 없음 |
| 14 | `stylists.display_name` 컬럼 없음 |

### B. UUID 체계 불일치 — 옛 스키마 잔재 (9건)

- `photographer_legacy_id`로 예약 조회 → 작가가 예약 요청을 못 봄
- `_customers` (존재하지 않는 테이블) → 알림 3종 전부 미발송
- `is_read` vs `read_at` → 알림 배지·읽음 처리 무력화
- `bookings.user_id` vs `customer_id` → 채팅방 생성 실패
- `Number(UUID)` → `NaN` → 예약 페이지 진입 불가
- `fetchPhotographers` 컬럼명 5개 오류
- `vendor_dresses`(VIEW)에 INSERT 시도
- 상품 저장은 jsonb, 로드는 테이블 → 저장한 상품이 사라짐
- `name`/`duration` vs `name_ko`/`duration_minutes`

### C. 데이터가 고객에게 도달하지 않음 (12건)

- 가입 시 `photographers`/`stylists` 레코드 미생성
- `is_active`를 true로 만드는 코드가 어디에도 없음
- 포트폴리오 저장이 DB 동기화 함수를 거치지 않음
- 벤더 `location_id` 입력 UI 부재 → 지역 필터에 영원히 안 걸림
- 정산 계좌가 localStorage 전용
- `artist_locations` 테이블 미사용 (다중 지역 저장 안 됨)
- 예약 화면이 작가 스케줄을 localStorage에서 조회
- 고객 마이페이지가 하드코딩 mock 3건만 표시
- 작가가 확정한 예약에 접근할 탭이 없음
- 헤메/의상/장소 선택에 mock 데이터 노출
- `location_names` 미저장 → 지역이 `seoul` raw id로 표시
- `photographers.packages`에 `hours`/`photos` 미저장

### D. 치명적 계산·렌더 오류 (7건)

| # | 내용 | 영향 |
|---|---|---|
| 1 | **결제 금액 1000배** — 문자열 덧셈(`"250000"+0+0+0`) | ₩250,000 → ₩250,000,000 |
| 2 | 작가 목록 전체 크래시 — DB row를 mock 형태로 렌더 | 페이지 진입 불가 |
| 3 | 첫 메시지 전송 시 채팅 크래시 (`LANGUAGE_NAMES_LOCALIZED['auto']`) | 채팅 사용 불가 |
| 4 | 예약 페이지 훅 규칙 위반 (early return 뒤 useState) | 조건부 크래시 |
| 5 | 시술 메뉴 저장 시 `{data,error}` 객체를 렌더 → 화면 멈춤 | 저장 불가 |
| 6 | 조회 함수 반환값 오용 3곳 → 목록이 항상 비어 보임 | 등록해도 안 보임 |
| 7 | `fmt()`가 undefined에서 `toLocaleString` 호출 | 렌더 크래시 |

### E. 정책·UX 오류 (14건)

- **예약 만료 안내가 실제 동작과 반대** ("자동 확정" ↔ 실제로는 자동 취소)
- 만료 시각에 촬영일 상한 없음 → 취소 확정 시점에 이미 촬영 하루 전
- 포트폴리오에 Unsplash 스톡 사진 자동 혼입 (저작권 문제)
- 헤메 대시보드 영어 고정 (`language` vs `lang`)
- 계정 전환 시 이전 역할이 세션에 잔존
- 업로드 실패 시 `blob:` URL을 DB에 저장 (새로고침하면 깨짐)
- 서비스워커가 새 배포를 차단
- 시술 가격이 `$` 표기
- 고객 예약 필터에 "대기" 상태 없음
- 포트폴리오 지역 필터가 미지정 게시글을 숨김
- 날짜 선택기가 Esc로 안 닫힘
- 벤더 프로필 "주소/지역" 자유 입력 → 표준 지역 ID와 매칭 불가
- 업로드 경로가 RLS 정책과 불일치
- `uploadImage` 반환 객체를 URL처럼 사용

---

## 검증 완료 상태

| 흐름 | 결과 |
|---|---|
| 작가 가입 → 프로필·상품·스케줄 → 고객 목록 노출 | ✅ 윤작가 · ₩250,000 · 서울 |
| 벤더 가입 → 업체 프로필 → 의상 등록 → 고객 노출 | ✅ 윤스튜디오 · 한복 ₩85,000 |
| 헤메 가입 → 프로필 → 시술 메뉴 → 고객 노출 | ✅ 윤헤메 · H&M ₩120,000 |
| **헤메 의상 대여 (신규 기능)** | ✅ 웨딩드레스 ₩150,000 |
| 고객 예약 6단계 (결제 직전까지) | ✅ 금액·시간·취소정책 정확 |
| 작가 예약 승인 → 고객 화면 반영 | ✅ |
| 작가 ↔ 고객 채팅 양방향 | ✅ |
| 알림 발송 + 벨 표시 | ✅ 미읽음 배지 포함 |
| **3자 결합 예약** (작가+헤메+의상) | ✅ 합계 ₩520,000 정확 |
| **리뷰 작성 → 공개 프로필 반영** | ✅ 후기(1) · ★5.0 |

---

## 남은 작업

### 사업자등록증 필요
- 토스페이먼츠 라이브 전환
- 정산 계좌 서버 저장 (민감정보 설계 포함)
- 카카오 비즈 앱 전환 (이메일 수집 동의항목)

### 별도 작업 필요
| 항목 | 이유 |
|---|---|
| 서비스워커 캐시 전략 | 배포해도 사용자가 옛 버전을 계속 사용 |
| `expireStaleBookings` 서버 스케줄 | 현재 작가 접속 시에만 실행 — 응답 없는 작가일수록 만료가 안 됨 |
| `artist_locations` 연동 | 다중 활동지역(국내3+해외3)이 저장되지 않음 |

### 요청받은 기능 (미착수)
1. 실명-휴대폰 일치 인증 — `profiles.real_name`/`phone` 컬럼은 준비됨
2. 이메일 인증번호 프로세스
3. 관리자 승인 + AI 자동 심사 — `approval_status` 컬럼 준비됨
4. 벤더 가입 주소 검색 — `ArtistRegister`에는 있으나 `VendorRegister`에는 없음

---

## 작업 규모 (1차)

- 커밋 47개
- 파일 38개 변경 (+2,405 / -369)
- SQL 마이그레이션 14개 작성

---
---

# 2차 검증 — 2026-09-11

1차에서 고친 뒤 예약 구조를 `booking_items`(예약 1건 : 아이템 N개)로 개편했다.
그리고 **실제 브라우저에서 실제 버튼을 눌러** 전 구간을 다시 돌렸다.

## 왜 다시 했나

1차 검증에서 "3자 결합 예약 ✅ 합계 ₩520,000" 으로 표시했는데,
그것은 **고객 화면의 금액 계산만 확인한 것**이었다.
그 예약이 헤메와 벤더에게 전달되는지는 확인하지 않았다.

실제로 확인해보니 전달되지 않고 있었다.

## 추가 발견 10건

| # | 문제 | 드러난 계기 |
|---|---|---|
| 62 | 헤메 서비스 메뉴 탭 크래시 (`lang` 미정의) | 탭 클릭 즉시 |
| 63 | 예약 STEP 05 크래시 (`venueVendors` 미정의) | 검사 스크립트 |
| 64 | **헤메·벤더가 자기 예약을 조회할 수 없음** | 헤메 대시보드 확인 |
| 65 | 예약 카드 `Invalid Date` · 고객 `N/A` · 시술 정보 없음 | 64 를 고치자 드러남 |
| 66 | 겸업 공급자의 의상 예약을 볼 화면이 없음 | 윤헤메가 의상 대여 겸업 |
| 67 | **`sendNotificationTo()` 가 한 번도 작동한 적 없음** | 작가 승인 버튼 클릭 |
| 68 | 승인 시 헤메·벤더 아이템이 `pending` 으로 잔류 | 승인 후 DB 확인 |
| 69 | 겸업 시 콜라보 인원 중복 계산 | 수수료 검산 |
| 70 | 벤더 `size_stock` 매핑 누락 → 재고 0 | "예약 가능 0" |
| 71 | **벤더 재고를 `Math.random()` 으로 생성** | 70 추적 중 |
| 72 | 벤더 예약 테이블이 mock 필드를 읽음 | 실제 예약 투입 |

### 62 · 63 — 미정의 변수 참조

62는 직전 커밋에서 내가 유입시킨 것이다.
`ServiceMenuTab` 에 `lang={lang}` 을 넘겼는데 그 컴포넌트는
`useLanguage()` 를 `language` 로 받는다.

63은 원래 있던 것이고 더 위험했다. mock 을 걷어낼 때 `venueVendors`
참조가 남아, **장소 벤더가 하나라도 등록되면 STEP 05 전체가 죽는** 상태였다.
장소가 0개라 렌더될 일이 없어 드러나지 않았다.

npm 레지스트리가 막혀 eslint 를 쓸 수 없어, babel 스코프 분석으로
정의되지 않은 식별자를 찾는 `scripts/check-undefined.mjs` 를 만들었다.
두 건 모두 이 스크립트가 잡았다.

### 64 — 헤메·벤더가 자기 예약을 볼 수 없었다 (RLS)

`booking_items` 는 정상 조회되는데 함께 가져오는 `bookings` 가 전부 null 이었다.
`bookings` 에는 고객·작가만 읽는 정책만 있었고, `getProviderBookings()` 는
조인 결과가 null 이면 건너뛴다.

코드는 전부 맞았고 **DB 정책 한 줄이 없어서** 헤메가 자기가 그날 일한다는
사실 자체를 알 수 없는 상태였다.

`is_booking_provider()` 를 SECURITY DEFINER 로 추가했다.
그러지 않으면 `bookings ↔ booking_items` 정책이 서로를 참조해 무한 재귀(42P17)가
난다. 1차의 1번(`profiles` RLS 무한 재귀)과 같은 함정이다.

### 67 — 알림이 한 번도 발송된 적이 없다

작가 계정에서 세 가지를 직접 시험했다.

| 시도 | 결과 |
|---|---|
| 작가 → 헤메에게 알림 발송 | **403 (42501 RLS 위반)** |
| 작가 → 헤메 아이템 상태 변경 | 200 이지만 **0행 갱신** |
| 작가 → 예약 전체 아이템 조회 | 자기 것 1개만 |

`notifications` 의 INSERT 정책이 `user_id = auth.uid()` 라서
**남에게 보내는 알림은 전부 막혀 있었다.** 작가→고객 확정 알림까지 포함해서다.
`.catch(() => {})` 가 삼켜 아무 흔적도 남지 않았다.

1차에서 "알림 발송 ✅" 로 표시했던 것은 **고객이 자기 자신에게** 보낸
알림이었다. 방향이 바뀌는 순간 전부 실패했다.

정책을 열면 누구나 아무에게나 알림을 보낼 수 있어 더 위험하다.
알림 발송은 원래 서버가 할 일이므로 SECURITY DEFINER 함수로 옮겼다.

```
approve_booking(uuid)        권한 확인 → 예약·아이템 확정 → 고객·공급자 알림
reject_booking(uuid, text)   아이템까지 취소해 일정을 되돌린다
notify_new_booking(uuid)     예약 생성 직후 공급자 전원에게
```

권한은 함수 안에서 확인한다. 확정·거절은 그 예약의 작가만,
새 예약 알림은 그 예약의 고객만 호출할 수 있다. 중복 처리도 막았다.

### 71 — 재고를 난수로 만들고 있었다

```js
mockInv[d.id][s] = {
  total:  Math.floor(Math.random() * 4) + 1,
  rented: Math.floor(Math.random() * 2),
};
```

목록을 불러올 때마다 총 재고와 대여 수를 새로 지어냈다.
실제 재고 계산 useEffect 가 덮어쓰지만, 그 전까지 벤더는 **존재하지 않는
숫자를 자기 재고로 보고 있었다.** 게다가 `size_stock` 매핑이 빠져 있어(70)
덮어쓴 값도 전부 0이었다.

## 무엇이 반복되는가

1차에서 정리한 네 가지 패턴(mock 폴백 / 조용한 실패 / 없는 컬럼에 쓰기 /
무조건 성공 표시)에 **다섯 번째**를 추가한다.

| 패턴 | 설명 | 이번 사례 |
|---|---|---|
| **RLS 가 의도대로 막는데 코드가 모름** | 정책이 정상 작동한 결과인데 클라이언트는 성공으로 처리 | 알림 403, 0행 갱신, 조인 null |

PostgREST 는 0행 갱신을 200 으로 돌려준다.
RLS 로 걸러진 조회는 빈 배열로 온다. 둘 다 에러가 아니다.
**"권한이 없어서 아무 일도 안 일어난 것"과 "정상 처리"를 구분할 수 없다.**

교훈: **권한이 걸린 쓰기 작업은 클라이언트에 두지 않는다.**
서버 함수에서 권한을 확인하고 처리하면 실패가 예외로 올라온다.

## 2차 검증 완료 상태

실제 계정 · 실제 데이터 · 실제 버튼으로 확인했다.

| 흐름 | 결과 |
|---|---|
| 헤메가 시술 시점 3종 등록 (선행·동행·상주) | ✅ DB 저장값까지 확인 |
| 고객 예약 화면에 시술 실제 시각 안내 | ✅ 14:00 / 16:30 / 15:00 |
| 촬영 시간 겹치는 헤메·장소 자동 제외 | ✅ 시술 단위로 판정 |
| 3자 예약 생성 → 참여자별 아이템 분해 | ✅ 수수료·실수령까지 |
| 작가 승인 → 예약·아이템 전체 확정 | ✅ RPC |
| 확정 알림이 헤메·벤더에게 도달 | ✅ 역할별로 각각 |
| 새 예약 알림이 공급자 전원에게 도달 | ✅ 2건 발송 확인 |
| 헤메 대시보드에 시술+의상 한 카드로 | ✅ 겸업 병합 |
| 벤더 대시보드 예약 테이블 | ✅ 날짜·시간·고객·품목·사이즈 |
| 벤더 재고 계산 | ✅ 예약 가능 0 → 1 |

## 2차 작업 규모

- 커밋 8개
- SQL 마이그레이션 4개 추가 (FIX_15 ~ FIX_18)
- 신규 모듈 2개 (`scheduling.js`, `scripts/check-undefined.mjs`)

## 2차에서 확인하지 못한 것

| 항목 | 이유 |
|---|---|
| 결제 (토스) | 클라이언트 키 미설정 — 사업자등록증 대기 |
| 채팅 · 리뷰 재검증 | 2차 범위 밖 (1차에서 확인, 구조 변경 영향 가능) |
| 장소(venue) 예약 전 구간 | 장소 벤더 데이터 0건 |
| 관리자 대시보드 | 여전히 mock 기반 |
